# Core (required) — the full pipeline runs with only these.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3

# Test
pytest>=7.0

# Optional enhanced semantic backend (NOT required).
# If installed, an embedding-based scorer becomes available; otherwise the
# pipeline falls back to a TF-IDF / lexical scorer and runs identically.
# sentence-transformers>=2.2
# nltk>=3.8
